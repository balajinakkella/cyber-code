//template which can be easily implemented in c++


#include<stdio.h>
#include<string.h>
#include<math.h>
#include<iostream>
#include<cstdio>
#include<cmath>
#include<cstring>
#include<stdlib.h>
#include<algorithm>
#define getcx getchar_unlocked
#ifndef ONLINE_JUDGE
    #define getcx getchar
#endif
#define MOD 329885391853
using namespace std;

inline long long int in()
{
   long long int n=0;
   long long int ch=getcx();long long int sign=1;
   while( ch < '0' || ch > '9' ){if(ch=='-')sign=-1; ch=getcx();}
 
   while(  ch >= '0' && ch <= '9' )
           n = (n<<3)+(n<<1) + ch-'0', ch=getcx();
   return n*sign;
}

int main()
{
    register long long int t,a,b,ans;
    t=in();
    while(t--)
	{
        
	}
    return 0;
}
